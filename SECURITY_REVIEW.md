# Security review and student worksheet
## Threat model
**Assets:** API key, GitHub account credentials, Linode root/LISH password, server access, service files/cache, availability, accurate learning content. **Trust boundaries/data flow:** laptop → GitHub (public repo) → Linode server (via LISH) → Nginx → Gunicorn/Flask → file cache → NVD/CISA. Treat browser inputs and external API text as untrusted, and treat the GitHub repository itself as untrusted-by-default since it is public. **Threats:** a secret committed to the public repository (the highest-likelihood threat in this workflow, since anyone can read it instantly), a leaked GitHub sign-in/PAT, a leaked Linode root/LISH password, excess firewall exposure, XSS, unsafe configuration, dependency weakness, API outage/rate limit, log/secret disclosure, permission errors.

## Controls and residual risk
Implemented: `.env` ignored and never present in the public repository; optional key is not logged/cached; parameters validate severity; Jinja escaping and JS `textContent`; timeouts/explicit exceptions/cache fallback; CSP, frame, referrer, MIME and permissions headers; localhost Gunicorn/non-root systemd; Nginx request limit; UFW plus Linode Cloud Firewall guidance, including closing port 22 to the public internet since access is via LISH, not SSH. Headers mean: `X-Content-Type-Options` prevents MIME guessing; `Referrer-Policy` limits referrer sharing; CSP limits allowed page resources; `Permissions-Policy` disables unused browser features; `frame-ancestors`/X-Frame-Options block framing. Residual: external data can be delayed/incomplete, app has no organisation asset inventory, file cache is single-server, HTTPS/domain setup is operational work, and a public repository can never fully protect against a student accidentally committing something sensitive — only discipline and `.gitignore` reduce that risk, they cannot eliminate it.

## OWASP mapping and checklist
A01 access: non-root/permissions; A02 crypto: HTTPS after domain; A03 injection: no database core, validate future queries; A05 configuration: headers/Nginx/firewall review; A06 components: pinned dependencies/update review; A07 auth: GitHub sign-in/PAT for pushes, Linode LISH password for server access — no SSH keys used anywhere in this workflow; A08 integrity: Git review, public commit history; A09 logs: journal/Nginx logs; A10 SSRF: fixed API URLs, no user URL fetches. Student sign-off: [ ] no `.env` committed to the public repository [ ] `git grep -n 'apiKey\|NVD_API_KEY'` reviewed [ ] keyboard/escaping test [ ] headers checked `curl -I` [ ] UFW and Linode Cloud Firewall both reviewed, port 22 closed externally [ ] service non-root [ ] updates planned via the laptop→push→LISH→pull loop [ ] stale-data warning seen [ ] rollback documented. Write: asset ___; threat ___; evidence ___; remaining risk ___; owner/date ___.
